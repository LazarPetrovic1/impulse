import styled from "styled-components";

const ChatSearchResultsContainer = styled.article`
  pointer-events: all;
  color: #111;
  position: absolute;
  z-index: 10;
  width: 100%;
`;

export default ChatSearchResultsContainer;
