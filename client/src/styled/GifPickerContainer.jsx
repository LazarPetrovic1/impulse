import styled from "styled-components";

const GifPickerContainer = styled.article`
  pointer-events: all;
  height: 400px;
  position: absolute;
  bottom: 45px;
  background-color: #eee;
  max-width: 485px;
  overflow-y: auto;
`;

export default GifPickerContainer;
